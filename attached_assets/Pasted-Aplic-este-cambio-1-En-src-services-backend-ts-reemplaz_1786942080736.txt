Aplicá este cambio:

1) En src/services/backend.ts: reemplazá getDraft(clubId)/saveDraft(state) por getWorldDraft(draftId)/saveWorldDraft(state) en la interfaz y en ambas implementaciones (modo local con localStorage, y modo Firebase con la colección "worldDrafts" en vez de "drafts", guardando/leyendo por state.id en vez de state.clubId).

2) En src/state/GameContext.tsx:
- Actualizá la importación de "@/game/youth" a los nuevos nombres: claimDraftSignings, closeWorldDraft, createWorldDraft, draftCanClose, draftPeriod, makeDraftPick, undoDraftPick (y los tipos DraftMemberSlot, DraftState). Sacá createDraft, refreshDraft, closeDraft si ya no existen.
- En el flujo de carga de sesión (loadEverything), sacá el bloque viejo que llamaba a backend.getDraft(c.id)/createDraft(c)/refreshDraft. En su lugar, INMEDIATAMENTE DESPUÉS de que "world" (la liga mundial) ya está cargada y con setWorld(wl) hecho, agregá: calcular period = draftPeriod(), draftId = `${wl.id}_DR${period}`, buscar con backend.getWorldDraft(draftId); si no existe, crear los members a partir de wl.members (mapeando a DraftMemberSlot con clubId=m.id, clubName=m.name, managerName=m.managerName, isAi=m.isAi, uid=m.ownerUid), y createWorldDraft({ id: draftId, country: c.country, period, members }), guardando con backend.saveWorldDraft; si ya existe pero draftCanClose(dr) es true, cerrarlo con closeWorldDraft(dr) y guardarlo. Llamá setDraft(dr) con el resultado. Si dr.phase === 'cerrado', reclamá fichajes propios con claimDraftSignings(dr, c, new Set(squad.map(p=>p.id)), squad.length, SQUAD_MAX) (SQUAD_MAX ya está importado de "@/game/market"): si devuelve jugadores, sumalos al array "squad" local, actualizá setRawPlayers, actualizá club.squadSize y setClub, y guardá cada jugador nuevo con backend.addPlayer más backend.saveClub.
- Reescribí pickProspect(prospectId), unpickProspect(prospectId) y finishDraft() para usar el nuevo modelo: pickProspect llama a makeDraftPick(draft, club.id, prospectId) y backend.saveWorldDraft; unpickProspect llama a undoDraftPick(draft, club.id, prospectId) y backend.saveWorldDraft; finishDraft ya NO cierra el draft (eso pasa solo automáticamente al llegar la fecha) — ahora solo reclama con claimDraftSignings lo que le corresponda a este club (igual que en loadEverything) y arma la noticia de fichajes si consiguió alguno, devolviendo { signed: number; lost: [] }.

3) En src/game/manager.ts, en la función de notificaciones, donde se usa draft.userPicks.length para la notificación de "Draft abierto", cambialo por (draft.picks[club.id]?.length ?? 0).

Al terminar corré npm run build y confirmame.